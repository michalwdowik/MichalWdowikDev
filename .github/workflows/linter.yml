name: Run Unit Tests and Lint Files
"on": push
jobs:
  unit_test-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
              npm install
              npm run lint


# name: Run Lint

# on:
#   push:
#     branches:
#       - main  

# jobs:
#   unit_test-lint:
#     runs-on: ubuntu-latest
#     steps:
#         uses: actions/checkout@v2
        
#         run: |
#                 npm install
#                 npm run lint
